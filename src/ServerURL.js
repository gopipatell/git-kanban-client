const serverURL = function (path) {
  // return "http://localhost:3000/" + path;
  return "https://git-kanban-server.herokuapp.com/" + path;
};

export default serverURL;

// import serverURL from "../ServerURL";
// serverURL(path);
